const initialState = {
  vacations: [],
  loading: true
}

const vacationsReducer = (state = initialState, action) => {
  switch(action.type) {
    default:
      return state;
  }
}

export default vacationsReducer;